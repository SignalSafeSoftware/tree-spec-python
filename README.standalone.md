# Standalone export

This tree was generated by `scripts/export-standalone-python-package.sh tree-spec` from the DeliveryPlus monorepo.

```bash
git init
git branch -M main
git add .
git commit -m "Sync tree-spec from DeliveryPlus"
gh repo create SignalSafeSoftware/tree-spec --public --source=. --remote=origin --push
```

Then follow `RELEASING.md` (when present) for PyPI trusted publishing.
